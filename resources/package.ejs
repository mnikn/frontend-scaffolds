{
  "name": "<%= name %>",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    <% if (lang === 'typescript') { %>"@types/lodash": "^4.14.120",<% } %>
    <% if (lang === 'typescript') { %>"typescript": "^3.4.5",<% } %>
    <% if (lang === 'typescript') { %>"awesome-typescript-loader": "^5.2.1",<% } %>
    <% if (type === 'app') { %>"webpack": "^4.29.5",<% } %>
    <% if (type === 'app') { %>"webpack-cli": "^3.2.3",<% } %>
    <% if (type === 'app') { %>"webpack-dev-server": "^3.2.0",<% } %>
    <% if (type === 'app') { %>"css-loader": "^2.1.0",<% } %>
    <% if (type === 'app') { %>"html-webpack-plugin": "^3.2.0",<% } %>
    <% if (type === 'app') { %>"style-loader": "^0.23.1",<% } %>
  },
  "license": "MIT"
}
  
  